import { ContactForm } from "../../components/ContactForm";

export const Contact = () => {
    return (
        <div id="contact" className="panel">
            <p>Contact section</p>
            <ContactForm />
        </div>
    );
};
